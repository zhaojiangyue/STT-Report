# Python
__pycache__/
*.py[cod]

# Output folder (don"t commit large audio/generated reports)
output/
*_results/

# Environmental variables (safety)
.env
.api_key

# Media files (avoid large binaries in repo root)
*.mp3
*.webm

# Local feed configs (may contain private URLs)
feeds.yaml

# IDEs
.vscode/
.idea/
